import { styled } from "@material-ui/styles";

const ChipRow = styled("div")({
  display: "flex",
  width: "100%",
  padding: 4,
  overflowX: "auto",
  flexFlow: "row nowrap",
  flex: "0 0 auto"
});

export default ChipRow;